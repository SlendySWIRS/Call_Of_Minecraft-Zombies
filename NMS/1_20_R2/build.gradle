archivesBaseName = "comz-1.20.r2-support"

dependencies {
    implementation project(':API')
    implementation group: 'org.spigotmc', name: 'spigot', version: '1.20.2-R0.1-SNAPSHOT'
}

repositories {
    mavenCentral()
    mavenLocal()
    maven { url 'https://repo.mikeprimm.com/' }
    maven { url 'https://maven.elmakers.com/repository/' }
    // maven { url 'https://repo.phoenix616.dev/' }
}